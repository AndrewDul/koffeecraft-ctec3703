<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardBackgroundColor="@color/kc_surface_panel"
    app:cardCornerRadius="22dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/kc_border_soft"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="124dp"
            app:cardBackgroundColor="@color/kc_surface_soft"
            app:cardCornerRadius="18dp"
            app:cardElevation="0dp"
            app:strokeColor="@color/kc_border_warm"
            app:strokeWidth="1dp">

            <ImageView
                android:id="@+id/ivLibraryImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Library image"
                android:scaleType="centerCrop"
                android:src="@drawable/kc_image_placeholder_generic" />
        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/tvLibraryLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="Espresso"
            android:textAlignment="center"
            android:textColor="@color/kc_text_primary"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>